# PPO self-play training dependencies (ppo_selfplay.py + fast_env.py + tests)
#
# torch is intentionally NOT listed here: install it FIRST, separately, with the
# CUDA build that matches the machine, e.g.
#   cu126:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#   cu130:  pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
# THEN install the rest:
#   pip install -r requirements.txt
#
# (Listing torch here would let pip pull the default PyPI wheel and clobber your
#  CUDA build. Lower bounds below avoid forcing downgrades of an already-good env.)
#
# The submission bot, submit_bot.py, needs ONLY numpy at runtime.

numpy>=2.0
PyYAML>=6.0
wandb>=0.27
